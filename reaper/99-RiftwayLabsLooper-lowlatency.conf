# RiftwayLabsLooper: lower PipeWire buffer for low-latency monitoring (~5 ms @ 48 kHz).
# Applies on next PipeWire restart / reboot. Remove this file to revert.
context.properties = {
    default.clock.quantum     = 256
    default.clock.min-quantum = 256
    default.clock.max-quantum = 512
}
